using UnityEngine;

public class BaseCreator : MonoBehaviour
{
    //[SerializeField] private Vector2Int _basePosition;
    //[SerializeField] private PlayerBase _basePrefab;
    //[SerializeField] private Ground _ground;

    //void Start()
    //{
    //    PlayerBase playerBase = Instantiate(_basePrefab);
    //    playerBase.transform.position = new Vector3(_basePosition.x, _basePosition.y);
    //    playerBase.Initialize(_ground.Playground);
    //    for (int x = -(playerBase.Size.x / 2); x < (playerBase.Size.x / 2)+1; x++)
    //    {
    //        for (int y = -(playerBase.Size.y / 2); y < (playerBase.Size.y / 2)+1; y++)
    //        {
    //            _ground.Playground.GetTile(_basePosition.x + x, _basePosition.y + y).Building = playerBase;
    //        }
    //    }
    //}
}
